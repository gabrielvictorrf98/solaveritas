source "https://rubygems.org"

# Use the GitHub Pages gem so versions stay compatible
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
  gem "jekyll-feed"
end
